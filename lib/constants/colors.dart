import 'package:flutter/material.dart';

class AppColors {
  // Fond principal clair, légèrement bleuté, pour une interface propre et douce
  static const Color background = Color.fromARGB(255, 234, 239, 250);

  // Couleur primaire : jaune ambré (#ffb619), syntaxe corrigée
  static const Color primary = Color(0xFFFF003C);

  // Couleur secondaire : violet moyen, pour boutons ou accents
  static const Color secondary = Color(0xFF000052);

  // Couleur tertiaire : bleu-gris foncé pour textes importants ou icônes
  static const Color tertiary = Color(0xFF37474F);

  // Couleur quaternaire : gris clair légèrement bleuté, pour fonds de cartes ou zones
  static const Color quaternary = Color(0xFFF5F5F7);

  // Blanc pur
  static const Color white = Color(0xFFFFFFFF);

  // Couleur d'alerte (erreur, warning)
  static const Color alert = Color(0xFFC62828);

  // Couleur pour bordures légères
  static const Color borderGray = Color(0xFFE0E0E0);

  // Couleur de texte principal (noir très foncé)
  static const Color textprimary = Color(0xFF212121);

  // Couleur de texte secondaire (gris moyen)
  static const Color textSecondary = Color(0xFF757575);

  // Fond clair pour containers, formulaires, champs
  static const Color containerBackground = Color(0xFFFAFAFA);

  // Accent violet vif professionnel (pour boutons, liens)
  static const Color accent = Color(0xFF673AB7);

  // Couleur complémentaire or/ambre pour accents supplémentaires (optionnel)
  static const Color complementary = Color(0xFFD4A017);
}
